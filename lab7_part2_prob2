#include <iostream>
using namespace std;
int reverseofnum(int n, int rev = 0) {
    if (n == 0)
        return rev;
    return reverseofnum(n / 10, rev * 10 + n % 10);
}
int main() {
    int n;
    cin >> n;
    cout << reverseofnum(n);
}
